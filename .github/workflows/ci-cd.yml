name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  placeholder:
    name: Placeholder pipeline
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print project status
        run: echo "CI/CD pipeline will be configured in a later step."